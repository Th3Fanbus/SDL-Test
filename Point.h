#pragma once
#include <SDL2/SDL.h>
#include <iostream>
#include <assert.h>
#include <vector>
#include <math.h>
using namespace std;

/*struct Point {
    const double x, y;
    Point(double x_, double y_): x(x_), y(y_) {}
	operator SDL_Point() {
		return {(int)x, (int)y};
	}
};*/

